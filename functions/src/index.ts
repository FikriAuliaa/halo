// Scheduled Cloud Functions. Currently a placeholder — the real
// cleanupExpiredReservations janitor (ADR-004, Phase 6) is implemented in
// B071. This file exists now purely so the Functions emulator declared in
// firebase.json has a valid codebase to load during local development of
// earlier phases; it exports nothing yet.
export {};
