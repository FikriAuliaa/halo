# Known Limitations (B139)

Every review in Phase 17 (`traceability-matrix.md`, `security-review.md`, `visual-fidelity-review.md`, `ux-review.md`, `performance-review.md`, `code-quality-review.md`) came back with **no blocking finding** — nothing that leaves the system's core correctness or security invariants unmet. What follows is the honest, non-blocking backlog those reviews surfaced, each with a severity and an owner, rather than quietly dropped.

| #   | Limitation                                                                                                                                                                                                                                                                                            | Severity                                                                                                                                                | Owner              | Notes                                                                                                                                                                                                                                                                     |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `adminOverrideOrderStatus`, `adminUpdateSystemConfig`, `adminManageAdmins` are declared in the `ADMIN_PERMISSIONS` matrix and referenced by `RUNBOOK.md` §3, but have no implemented operation or route.                                                                                              | Medium (operational, not a security gap — there's no privilege-escalation risk from a capability that doesn't exist)                                    | Product/Eng        | Before relying on `RUNBOOK.md` §3's manual-correction procedure in a real incident, either implement `adminOverrideOrderStatus` or rewrite that runbook section to describe the actual available recourse (direct, audited DB correction by an engineer, outside the app) |
| 2   | Package prices remain `price_status: draft` (OQ-1) and the QRIS image is a dev placeholder, never scan-verified on a real device (OQ-6).                                                                                                                                                              | **Blocking for production launch specifically**, not for this build phase — both were already scoped this way from the original open-questions register | Business/Telkomsel | Both explicitly gate `B140`'s production readiness sign-off below; neither is a code defect                                                                                                                                                                               |
| 3   | The tracking-token comparison (`findByRefAndTokenHash`) relies on a SQL `WHERE` equality predicate rather than an explicitly constant-time comparison function; reasonable in practice (the DB-side comparison isn't the common naive-JS-`===` timing vector) but never independently timing-audited. | Low                                                                                                                                                     | Security           | Worth a dedicated timing-attack proof-of-concept before this handles real payment evidence at scale                                                                                                                                                                       |
| 4   | Core Web Vitals under real throttled-3G/mid-tier-CPU conditions were never measured — this environment has no Lighthouse/CDP-throttling tooling installed.                                                                                                                                            | Low                                                                                                                                                     | Eng                | Real bundle sizes and unthrottled local timing were measured instead (`performance-review.md`) and showed no red flag, but this isn't a substitute                                                                                                                        |
| 5   | No true pixel-fidelity comparison against the original five reference screenshots exists, because those images were never supplied to this repository at any point in the build.                                                                                                                      | Low                                                                                                                                                     | Design             | If the originals surface later, `e2e/visual.spec.ts`'s baselines are the natural place to diff against them                                                                                                                                                               |
| 6   | The reservation countdown starts at number-selection, before package/data entry — a fast typist has ample time, but this hasn't been observed under real student usage.                                                                                                                               | Low (product observation, not a defect)                                                                                                                 | Product            | Monitor via the `oldest_pending_order_age_minutes`/`reservation_failed` metrics in `docs/monitoring.md` once deployed                                                                                                                                                     |
| 7   | Operational readiness (B129-B131: staging/production deployment, rollback rehearsal, backup/DR) was never exercised — no real cloud project has existed at any point in this build.                                                                                                                   | High for a production launch, N/A for this build phase                                                                                                  | Eng/Ops            | See `docs/reports/phase-16-gate.md` for the full reasoning; this is an environmental gap, not a skipped task                                                                                                                                                              |

## What was fixed, not deferred, during Phase 17 itself

- `AGENTS.md`'s two remaining Firestore-era prohibition bullets, corrected to their real Postgres equivalents (`code-quality-review.md`).
- `RUNBOOK.md` §6 ("Unreachable Firestore" → "Unreachable Postgres/Supabase"), corrected in Phase 16.

## Verdict

No blocking finding survived Phase 17's reviews. The backlog above is genuine and owned, not hidden — `B140`'s production readiness checklist below reflects every one of these items honestly rather than signing off around them.
